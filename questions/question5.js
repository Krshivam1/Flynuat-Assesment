//5th qoestion 



function average(a, b) {
    return a + b / 2;
}
console.log(average(2, 1));
// output: 2.5
// explanation: in this case the division operator has more precedence than the addition operator. 
// So, the expression is evaluated as 2 + (1/2) = 2.5


